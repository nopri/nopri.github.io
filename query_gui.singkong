reset()
var t = component("table", "A,B", true)
add(t)

var d = database("org.h2.Driver", "jdbc:h2:/tmp/test", "admin", "admin")
if (database_connected(d)) {
    var q = [ ["select a,b from test", []] ]
    var r = query(d, q)
    if (!empty(r)) {
        config(t, "contents", r[0])
    }
}

show()
